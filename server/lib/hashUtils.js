const crypto = require('crypto');

/************************************************************/
// Add any hashing utility functions below
/************************************************************/

var createHash = function(passward) {
  var hash = 'hashfromcreateHash';
  return hash;
  // var salt = Math.
}